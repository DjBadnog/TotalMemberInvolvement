<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.example.abnn965.totalmemberinvolvement.HomeActivity"
    tools:showIn="@layout/activity_home">

    <ImageView
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:id="@+id/imageView2"
        android:background="@drawable/logo"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:id="@+id/btnCaptureTargetActivity"
        android:layout_below="@+id/imageView2"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="59dp"
        android:background="@drawable/gradient_bg_round"
        android:textColor="#fff"
        android:textSize="20sp"
        android:onClick="onClickCaptureTarget"
        android:hint="Capture Target"
        android:textColorHint="#fff" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:id="@+id/btnViewTargetsActivity"
        android:layout_below="@+id/btnCaptureTargetActivity"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/gradient_bg_round"
        android:textColor="#fff"
        android:textSize="20sp"
        android:textColorHint="#fff"
        android:hint="Check In Target" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="65dp"
        android:id="@+id/btnCheckInTargetActivity"
        android:layout_below="@+id/btnViewTargetsActivity"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/gradient_bg_round"
        android:textColor="#fff"
        android:textSize="20sp"
        android:hint="View Target"
        android:textColorHint="#fff" />

</RelativeLayout>
